import { RawLicense, RawTransaction } from "./raw.js";

export function normalizeLicense(license: RawLicense): RawLicense {
  return license;
}

export function normalizeTransaction(transaction: RawTransaction): RawTransaction {
  return transaction;
}
